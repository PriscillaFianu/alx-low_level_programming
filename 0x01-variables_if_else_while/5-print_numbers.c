#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: Prints all alphabets in lowercase
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int n;

	for (n = 0; n <= 9;)
	{
	printf("%d", n);
	}
	printf("\n");
	return (0);
}
